{{- if .Values.opsaiAutofix.enabled }}
{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{.Values.opsaiAutofix.clusterRoleBinding.name}}
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{.Values.clusterRole.name}}
subjects:
  - kind: ServiceAccount
    name: {{.Values.opsaiAutofix.serviceAccount.name }}
    namespace: {{ .Release.Namespace }}
{{- end }}
{{- end }}
